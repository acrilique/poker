[package]
name = "poker-core"
version = "0.1.0"
edition = "2024"
description = "Shared poker types: card evaluation, hand ranking, and wire protocol"
license = "MIT"

[dependencies]
rand = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
